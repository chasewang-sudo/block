# Block Rise: Whack-a-mole 项目代码说明文档

## 一、 项目总览

本项目是一个基于**“方块消除（Block Blast）”**基础玩法并融合了**“打地鼠（Whack-a-mole）”**机制的混合型游戏。
代码仓库主要包含了三个维度的内容：
1. **纯前端 Web 游戏主体**：完全由 HTML/CSS/JS 构成的移动端网页游戏，视觉细节丰富（包含丰富动效、反馈特效、Combo 展示等）。
2. **Python 数据仿真引擎**：一个用于模拟游戏逻辑的 Python 脚本群，内建了多种决策 AI 策略，可提供自动化游戏关卡模拟、难度配置验证与分析报表服务。
3. **麻将玩法原型（历史探索）**：利用独立 JS 和 CSS 实现的“麻将饼子”方块消除原型，相对主干代码属于单独的实验性分支。

---

## 二、 核心文件组成与功能梳理

### 1. 游戏主程序：`index.html`
**文件大小：** 约 3546 行代码（合并了结构、样式与逻辑）。  
**定位：** 本项目的可玩客户端入口。  

- **UI 设计与样式 (`<style>`)**
  - **CSS 变量 (CSS Variables)：** 在 `:root` 中统一定义了调色板（`--bg-0` 到 `--bg-2` 等变量），用于维持项目色彩统一和支持暗黑/明亮主题扩展。
  - **响应式布局：** 使用 CSS Grid (如 `#game-grid`) 实现了 8x8 的完美贴合方块棋盘；使用 Flexbox 统筹了顶部状态栏和底部候选块容器 `#pieces-container`。
  - **关键动画 (Keyframes)：** 定义了大量细腻动效，如方块消除的 `clear-pop`、碎裂效果 `shatter`、地鼠受击反馈 `mole-stun`、以及多段连击悬浮提示文本（Combo, Toast 等）的动画。

- **游戏逻辑层 (`<script>`)**
  - **状态管理：** 维护了棋盘状态数组 (8x8)、玩家分数、得分目标、时间限制、以及地鼠实体列表 (Moles)。
  - **核心机制 – 方块拖拽与放置：** 监听 `touchstart/touchmove` 以及鼠标事件，实现了方块从底部托盘拖动到棋盘的高亮预判和放置吸附逻辑。
  - **核心机制 – 消除判据：** 在方块放置后检测行列是否被填满（行列 `every` 为 `true`），若满足条件则清空数据，并触发 HTML 元素的消除动画。
  - **核心机制 – 打地鼠 (Whack-a-mole)：** 在特定的空位或者被方块填满时随机/按规则刷新地鼠。如果被消除的方块正好覆盖了地鼠，触发锤子（Hammer）的敲击动画，并产生奖励动画和 Combo 加成计算。
  - **反馈与提示 (Toast System)：** 包括普通提示、完美的连击弹窗（“Perfect Timing” 等）的逻辑分发与视图挂载。

### 2. 数值仿真与 AI 框架：`simulator.py` 和 `simulator_web.py`
**文件大小：** `simulator.py` ~1036行；`simulator_web.py` ~613行。  
**定位：** 数值策划与关卡验证的核心后勤组件，确保关卡不卡死以及返奖率评估。

- **`simulator.py`：** 
  - **规则镜像引擎：** 使用 Python 重写了 JS 的方块状态、消除逻辑和地鼠游走、冻结（`freeze_turns`）逻辑。由于用于大规模计算，去除了视图耦合。
  - **AI 决策组 (Policy)：** 包含四种代际的 AI 策略，模拟玩家行为去下棋。
    - **`capture_greedy` (贪心捕手)**：倾向于直接消除和抓取。
    - **`survival_mobility` (生存机动)**：旨在最大限度保证棋盘空旷，确保能放出后续的大块形状，从而存活更久。
    - **`lookahead_2ply` (双步前瞻)**：探索未来两步棋的决策树。
    - **`triplet_beam` (三块束搜索)**：以每次刷新的三个方块为单位，进行束搜索找出最高收益的放置顺序组合。
  - **随机种子与难度生成 (`seed` / `difficulty`)：** 根据关卡配置提供一致性的随机流（Random Stream）。

- **`simulator_web.py`：**
  - 基于 `BaseHTTPRequestHandler` 的轻量级本地控制台面板。
  - 提供了一个可视化的仿真网页报表工具，能将 `simulator.py` 的仿真结果转出为直观的 HTML 表格或 CSV 报告，辅助数值策划（Game Design）对关卡的胜率、地鼠出现率、预估耗时进行动态微调。

### 3. 麻将原型脚本：`script.js` 和 `styles.css`
**定位：** 之前的特殊主题游戏 Demo (`Block Blast Clone`)。
- `script.js` ：使用前端原生的 `document.createElement` 等 DOM 操作完成了一套简化版的 8x8 消除逻辑。由于它将方块纹理替换成了 1 饼到 9 饼，并带有收集所有饼子的特殊进度条。
- 在当前项目中属于平行参考代码或等待废弃的代码模块，与主项目 `index.html` 的结构是互斥且独立的。

---

## 三、 代码结构质量评价

### 优点：
1. **动画交互手感极为扎实（Juiciness）**：`index.html` 配合了精细化的大量 CSS3 动画类名，并使用了 `.crack`、`.shatter` 等小颗粒度的震屏反馈，交互体验处于网页游戏的很高水准。
2. **前后端架构清晰（业务和工具分离）**：主包虽然单体化塞进了一个 HTML 用以快速部署移动端 H5 场景，但在 Python 端建立了一套极其完整的影子引擎来进行关卡验证，这是非常科学与工业化的研发流程。
3. **数据结构清晰可扩展**：使用了明确的 `Block_id` 字典，能够轻松扩展几十种不同的方块矩阵组合；对于随机分布以及概率调整留有良好的常量入口（如 `MAHJONG_PROB`、`MOLE_REWARD_RATE`）。

### 可改进项（隐患与优化建议）：
1. **单文件体积过大（`index.html`）**：该页面目前接近 3600 行代码将所有层（表现层、样式层、数据层）揉搓在一起。随着持续迭代和功能堆积（例如你刚才需要调整的 Perfect Timing 逻辑），会导致代码在合并或修改时极其容易发生由于疏漏造成的连锁 Bug。
   - **建议：** 引入 Webpack/Vite 这样的构件工具，将 CSS 拆分到专门的样式表中，将 JS 按实体（如 `Board.js`, `Mole.js`, `Draggable.js`, `ScoreSystem.js` 等）进行模块化分离。
2. **无用代码清理**：如果 `script.js` 和 `styles.css` 已被放弃，建议移动至归档分支或物理删除以保障主分支的纯净。

## 四、 后续改动指南
既然您建立了一个新的 `dev-update` 分支：
- **修改表现与动效**：请搜索 `index.html` 内的 `<style>` 标签或查阅其中的 `@keyframes`。
- **改动交互与判定**：检索 `index.html` 内侧后半部分的 `<script>` 标签内包含的 `document.addEventListener` 相关拖拽生命周期。
- **调整游戏难度数值**：如果需要保证数值的平稳，需要同步修改 Python 的 `simulator.py` 用于自动化重测，确保你的任何难度拉高不会被束搜索算法判定为“死局”。

## 五、 详细游戏规则与核心系统设计

### 1. 基础方块消除 (Block Blast)
- **棋盘配置：** 游戏使用 $8\times8$（`GRID_SIZE = 8`） 的矩形网格。
- **全局时限配置：** 默认软限制时间为 180 秒（`TIME_LIMIT = 180.0`），无限制模式下此值为 -1。
- **生成机制：** 每次从底部面板随机刷新三个形状不一的源方块。生成受内置区块形状字典的 `id` 白名单、黑名单（`MOLE_BLACKLIST` 常过滤如全填满大方块等特殊极值块）以及概率流控制。其中，系统使用异或散列生成随机种子产生稳定的 Pseudo-Random 流来出块。
- **放置与消除判定：**
  - 玩家点击并拖拽方块，悬停至网格会自动显示高亮投影提示（可放或不可放）。
  - 放下后，若完整填满水平行或垂直列，触发消除动作。
  - **死局判定：** 如果面板上剩余的任何一块候选方块都无法放进目前 $8\times8$ 网格内的可用空隙，即立刻触发对局结束重置（ Fail 状态）。

### 2. 打地鼠附加机制 (Whack-a-mole)
- **实体状态：** 地鼠被方块遮蔽后会进入 `freeze_turns` 状态停止游走 1 步。其具有“随机游走”、“等待”、“受惊”和“被敲晕（Stunned）”等视觉反馈状态。由于地鼠跟随消除产生交互，不捕捉它时它会根据 `move_moles` 逻辑沿着留空的区域（`holes` 数组）向周边 4 联通空白格游走。
- **生成规则 (Spawn)：** 默认情况下，地鼠基础出现率配置为 30% 到 40% 之间（常量中为 `MOLE_SPAWN_RATE = 0.40`），但在 `segment_35_30_20_5` 阶段模式下，游戏进程被硬分为几个序列阶段：
  - 第 1-3 步（前期）：强制产出 100%（`rate = 1.0`）确保必定刷出地鼠。
  - 第 4-30 步（过渡）：降低至 `0.30`。
  - 第 31-60 步：产出率进一步缩减 `0.25`。
  - 第 61-90 步：`0.20`。
  - 90步之后大后期：极难出现，只有 `0.05` 概率。
- **防摆烂保护 (Guardrails):** 具备一个隐式逻辑，前三步强制在最大的可用方块格子上刷新至少一只地鼠（前提是白名单内）。
- **捕获与敲击：**
  - 当发生整行/列消除并且覆盖的格子上有一只地鼠实体时，游戏生成“被击中 (Hit/Smash)”判定。捕获到的地鼠算作得分，剩余的存活地鼠将继续保留在未消除的空洞。
- **过关目标与上限：** 每局常规过关目标默认地鼠数（`goal_target = 12`），并且全图同屏存活最大的地鼠数封顶默认限制为 20 只。

### 3. 多重连击与奖励计算系统 (Score & Combo)
- **连击分层 (Combo System)：** 同一次放置同时消除两行、三行甚至更多级别递增（Double = 2行，Triple = 3行等），并带有全屏粒子特效 `combo-burst`。
- **经济倍率 (Economy Structure)：** 
  - 根据常数设定，消除时基础的抓鼠经济预期 `MOLE_REWARD_RATE = 0.1`。
  - 下注杠杆机制 `FIXED_RETURN_MULTIPLIER = 2.0`。如果单场下注金额 `entry_fee = $10`，抓到一只地鼠可能对应 $1，同时系统存在一个 Max Reward 封顶，即 `maxMolesCap * entry_fee * reward_rate`。
  - 这套模型配合四套 AI 策略测试：`capture_greedy`(1000分优先吃鼠) 和 `triplet_beam`（最强 3 块前瞻防死局生存打法，适应度较高）来进行内部回本率 RTP 测算。

### 4. 极致交互与被动动效反馈系统 (Juiciness & Interactions)
- **手势拖拽偏移：** 为解决移动端手指遮挡视线，在 `dragStart` 时存在 `TOUCH_LIFT_PX = 200`（内部按比例缩放）的向上吸附偏移量，保证玩家放下积木的精准度。
- **拖拽预判高亮区 (Ghost / Preview)：** 根据被托起积木相对于游戏网格的绝对坐标求逆转算出 $row/col$，即时通过 `cell.classList.add("preview")` 进行反馈。若放置判定冲突，颜色则会闪烁转为带红色预警的 `invalid-flash` 并在放手时触发 `rebound` 回原位。
- **消除微震感 (Camera Shake)：** 
  - 发生单次普通消除时，仅仅对单个 cell 容器增加 `.crack` 类触发时间长达 `0.18s` 的自身粒子散光动画。
  - 对于地鼠捕捉和连击达到一定数量，系统会触发游戏外壳级别 DOM 的全局震动动画（Screen Shake），持续 `0.25s`，幅度 `±3px`。
- **动感反馈：锤子与粒子体系：** 捕捉地鼠触发独立层 `.hammer` 下落（`hammer-smash`），时长 `0.35s`，并立刻带有中心蛋壳碎裂（`egg-shatter`）、数字飘动（`reward-fly`）及金币上涌轨迹（`money-fly`）动效脱离文档流直至回收。并在连击高潮处通过 JS 创建固定绝对布局的随机四散彩色 `.combo-particle` 强化胜利快感。

## 六、 进阶：可优化与改进方向

考虑到 `index.html` 这一单页面承载了极大的界面逻辑和业务数据流，目前在系统可维护性和部分性能环节上仍然存在可以深度优化的空间。

### 1. 架构调整与模块解耦 (Architecture Refactoring)
- **CSS 抽离与作用域分离：** `index.html` 头部的 `style` 规则多达 800 余行，其中包含大量硬编码颜色变量和独立动画关键帧。建议将其抽离为基础变量 `theme.css`、布局 `layout.css` 和独立动画层 `animations.css`。
- **JS 面向对象重构：** 将现有的 `App` 全局散装状态封装剥离，改用 ES6 Class。例如可抽离出：
  - `GridManager.js`：专职处理 8x8 棋盘的二维数组判断与方格状态。
  - `MoleEntity.js`：管控地鼠游走、冻结状态机及关联动效。
  - `DragController.js`：统筹触摸滑动、屏幕反馈、吸附阻尼体系。
- **状态流转机制：** 在核心循环中引入类似 Event Bus 之类的事件触发机制，解耦拖拽结果（由 Controller 事件传递到 ScoreSystem 或者 LevelSystem 中做加分结算）。

### 2. 渲染性能保护 (Performance Baseline)
- **重绘开销管理 (DOM Reflows/Repaints)：** 大量生成的全屏漂浮 `combo-burst` 和粒子效果当前可能仍然采用频繁拼接 DOM 元素方案。在多联排大清空时，激增 DOM 节点会导致主线程绘制阻塞。建议建立基于 DOM 的“对象池（Object Pool）”来接管这些飞翔的数值与特效片段；如遇不可解卡顿则考虑将核心特效逻辑挪给 HTML `<canvas>`。
- **TouchMove 节流处理 (Throttling)：** `touchmove` 在频繁寻找当前手指对应悬停方格以展现高亮（Preview）时负担较重。可在查表预测这块加入 `requestAnimationFrame` 防抖限制，消除移动端低端机滑动卡顿。 

### 3. 反作弊安全 (Cheating Prevention)
- **前后端一致性审计：** 如果项目落地含有真金（Real-Money）抽逃，目前的客户端运算结果需被服务器严格重演。依托当前 `simulator.py` 中精准构建的一致性预测规则（包括相同的种子算法 `deterministic_roll01` 与下注难度模型），未来联调时必须将玩家拖放序列编码为 payload，在后台进行同步解壳来保证下注无修改器入侵可能。

---

## 七、 新手工程师快速上手指南 (Quick Start)

### 1. 运行前端可视版本
由于主干包为纯静态 `index.html` 构架，没有任何外部服务器依赖：
1. 请直接在 VS Code 中安装 **Live Server** 插件。
2. 并在项目根目录右键点击 `index.html`，选择 **"Open with Live Server"**。
3. 也可以直接把 `index.html` 拖入 Chrome 浏览器中运行游玩。
*(注意：请开启浏览器的设备仿真模式（F12 -> Toggle Device Toolbar）切换为 iPhone 12/14 尺寸，以达到最佳响应式 UI 体验。)*

### 2. 运行 Python 后端数值仿真测算
如果您是数值策划或是接手经济系统的开发，需要测试关卡机制概率（确保不死局，返奖率达标）：
1. 确保系统安装了 **Python 3.8+**。
2. 在终端（Terminal）进入项目根目录。
3. 若要快速进行纯数值测算（控制台输出）：
   ```bash
   python3 simulator.py --runs 1000 --policy triplet_beam
   ```
4. 若要开启可视化的仿真调试后台：
   ```bash
   python3 simulator_web.py --port 8080
   ```
   随后在浏览器访问 `http://localhost:8080/`，即可在页面上可视化生成地鼠概率和关卡难度的分析报表。

> **小结与叮嘱：** 本项目兼具了前端 H5 原生动效打磨的细腻度，以及 Python 后端仿真算法的深度。新手参与时，如果只是修改 UI 文案或前端微调，直接检索修改 `index.html` 即可；如果是触碰到底层掉落、概率或者难度机制，切记一定要使用 `simulator.py` 跑批次验证 RTP (Return to Player) 的安全红线。
